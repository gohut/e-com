export default function DashboardSettingsPage() {
  return (
    <main style={{ padding: "24px" }}>
      <h1>Dashboard Settings</h1>
      <p>This is the settings route (`/dashboard/settings`).</p>
    </main>
  );
}
